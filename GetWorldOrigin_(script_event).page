GetWorldOrigin (script event)
=============================

Game class
----------

This event operates on an instance of the [Game\_class.html game class] type {{{gameclass}}}

Description
-----------

Returns the current worldspace position of this entity (regardless of any bind parent).

Usage
-----

*vector* getWorldOrigin()

Parameters
----------

This script event does not accept parameters.

Examples
--------

` `
`postition = `<font color="#000055">`$my_entity`</font>`.`<font color="#ff0000">`getWorldOrigin`</font><font color="#000000">`(`</font><font color="#000000">`)`</font>`;`

Notes
-----

This script event returns the worldspace postition of this entity as a vector.


